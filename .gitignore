# Fotos de candidatos (~92k archivos, 1.7 GB) -> se hostean en Backblaze B2, NO en GitHub.
# Ver deploy/README-cdn-fotos-b2.md
assets/fotos/

# macOS
.DS_Store

# Dependencias del Worker (si instalas wrangler localmente)
node_modules/

# --- Archivos de trabajo / fuentes que NO se sirven en la landing ---
# Hojas de vida (PDF) de candidatos usadas solo para completar la data.
CANDIDATOS-LIMA-FALTAN-HDV/

# Planes de gobierno en PDF (~42 MB) descargados del JNE, no forman parte del sitio.
planes_gobierno_pdf/
PLANES_DE_GOBIERNO_FASE3_fix.json

# Spreadsheets fuente / generados (la landing consume assets/data/*.json, no .xlsx).
*.xlsx

# Respaldos y temporales
*.BACKUP.json
*.bak
*.tmp
